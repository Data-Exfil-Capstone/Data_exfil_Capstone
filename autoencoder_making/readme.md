The purpose of this folder is to document and connect all the peices of the autoencoder. 

The idea is having a folder for each aspect of the autencoder so we can easily edit the autencoder to see what works the best .

Ie having a nodes section with various nodes.py files that include the different neuron stuructues that we might use and a equation folder including the different py files that use diffenet equations between each layer to see which one works the best. 

Could move the folder further into the idea that can run the trainer using - to dictate which aspects would be prefereed. 
